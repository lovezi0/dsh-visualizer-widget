# dsh-visualizer-widget bundle patch：一个 insert 注册本插件。
# insert.name 必须是【包名】（dsh plugin add 后由 profile 的 node_modules 解析到 lib/index.js）；
# insert.id 必须与入口导出的 cordis `name`（'visualizer-widget'）一致。
- insert:
    - id: visualizer-widget
      name: 'dsh-visualizer-widget'
